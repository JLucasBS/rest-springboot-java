spring:
  application:
    name: rest-springboot-java
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/rest_springboot_java?useTimezone=true&serverTimezone=UTC
    username: developer
    password: jlucasbs
  jpa:
    hibernate:
      ddl-auto:  none
    show-sql: false
    open-in-view: false
  flyway:
      baseline-on-migrate: true

logging:
  level:
    root: warn
    dev.jlucasbs.study: debug

spring-doc:
  paths-to-match: /api/v1/**/**
  swagger-ui:
    use-root-path: true

